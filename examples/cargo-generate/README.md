# Lazygit

A simple example which makes [cargo-generate] available in `rv` at runtime. Run this example with:

```bash
nix run ./examples/cargo-generate
```

[cargo-generate]: https://github.com/cargo-generate/cargo-generate
