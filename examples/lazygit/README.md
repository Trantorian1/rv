# Lazygit

A simple example which adds [lazygit] support to `rv`. Run this example _in a git repository_ with:

```bash
nix run ./examples/lazygit -- -- -c ":LazyGit"
```

[ lazygit ]:  https://github.com/jesseduffield/lazygit
